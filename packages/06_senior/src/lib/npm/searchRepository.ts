import { Options } from '../methods';

async function searchRepository(options?: Options): Promise<{ [key: string]: string }> {
    return Promise.resolve({});
}

export {
    searchRepository as default,
};
