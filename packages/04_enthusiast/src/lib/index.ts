import { Stream } from './Stream';
import { Readable } from './Readable';
import { Writable } from './Writable';

const enthusiast = {
    Stream: Stream,
    Readable: Readable,
    Writable: Writable
};

export = enthusiast;
